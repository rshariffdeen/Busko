{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Drivers list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Driverlicencenumber</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('drivers_show', { 'id': entity.id }) }}">{{ entity.driverLicenceNumber }}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('drivers_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('drivers_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('drivers_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
