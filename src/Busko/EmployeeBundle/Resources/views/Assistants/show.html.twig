{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Assistants</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Machinenumber</th>
                <td>{{ entity.machineNumber }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('assistants') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('assistants_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
