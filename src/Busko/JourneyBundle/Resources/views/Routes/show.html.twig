{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Routes</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.routeId}}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('routes') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('routes_edit', { 'id': entity.routeId }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
