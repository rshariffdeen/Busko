{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Routes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('routes_show', { 'id': entity.routeId }) }}">{{ entity.routeId}}</a></td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('routes_show', { 'id': entity.routeId }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('routes_edit', { 'id': entity.routeId }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('routes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
